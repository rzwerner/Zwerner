[build]
  command = "npm run build"
  publish = "unsettled-agitators-pwa/dist"

[build.environment]
  NODE_VERSION = "18.18.0"
  PYTHON_VERSION = "3.11.8"  # or another currently supported stable version
